# Oblakogroup port checker
### How to build
* Install latest stable rust version from https://rustup.rs/
* Clone project and run `cargo build --release`
* Executable will be in `./target/release/port-checker-client`